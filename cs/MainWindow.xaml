<Window x:Class="MessengerFreezeTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MessengerFreezeTest"
        mc:Ignorable="d"
        Title="Messenger Freeze Test" Height="200" Width="400">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0"
                Content="HeavyWork 메시지 보내기"
                Padding="10"
                Margin="0 0 0 10"
                Command="{Binding SendHeavyWorkCommand}"/>

        <TextBlock Grid.Row="1"
                   Text="{Binding Status}"
                   FontSize="16"/>

        <TextBlock Grid.Row="2"
                   Margin="0 20 0 0"
                   Text="버튼 누른 뒤 3초 동안 창을 드래그하거나 리사이즈 해보세요.
UI가 먹통처럼 느려지면 Receive에서 heavy work가 UI 스레드를 막고 있다는 뜻입니다."
                   TextWrapping="Wrap"/>
    </Grid>
</Window>
