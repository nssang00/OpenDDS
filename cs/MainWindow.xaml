<Window x:Class="MessengerHeavyTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MessengerHeavyTest"
        mc:Ignorable="d"
        Title="Messenger Heavy Test" Height="200" Width="400">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <StackPanel Margin="20" VerticalAlignment="Center">
        <Button Content="Heavy Receive 테스트 시작"
                Command="{Binding StartHeavyTestCommand}"
                Height="40"
                Margin="0,0,0,10" />

        <TextBlock Text="{Binding Status}"
                   FontSize="16"
                   TextWrapping="Wrap" />
    </StackPanel>
</Window>
