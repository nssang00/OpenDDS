// rulesToStyleFunction 예시
const rules = [
  { filter: ['==', 'property', 'value'], style: new Style({ /* 스타일 정의 */ }) },
  { filter: ['!=', 'property', 'value'], style: new Style({ /* 다른 스타일 정의 */ }) }
];
const styleFunctionFromRules = rulesToStyleFunction(rules);

// flatStylesToStyleFunction 예시
const flatStyles = [
  { fill: 'red', stroke: 'blue', strokeWidth: 2 },
  { fill: 'green', stroke: 'yellow', strokeWidth: 1 }
];
const styleFunctionFromFlatStyles = flatStylesToStyleFunction(flatStyles);

createRulesToOlStyle

createOlStyle
